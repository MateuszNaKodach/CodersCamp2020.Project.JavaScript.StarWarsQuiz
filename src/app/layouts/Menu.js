export const Menu = (image) => {
  console.log('Działa Menu!');
  const menuDomObj = document.createElement('div');

  // image = atob(image);

  // imageWrapper.classList.add('swquiz-app-image');
  // imageWrapper.style.backgroundImage = `url(${image})`;
  // app.appendChild(imageWrapper);

  return menuDomObj;
};
